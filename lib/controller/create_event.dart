import 'package:gestion_ecurie/services/insert_event.dart';

createAnEvent(nomconcours, specialite, adresse, date, photo, username) {
  insertEvent(nomconcours, specialite, adresse, date, photo, username);
}